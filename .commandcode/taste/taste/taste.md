- For runtime/binary installers shipped to end users, prefer a single-command auto-install on first use (e.g. when the user first picks a feature that needs the runtime) over a separate "Install runtime" button — but also surface an explicit "Install" / "Reinstall" button in settings for cases where auto-install was dismissed, failed, or the user wants to swap builds. Default = friction-free; escape hatch = always available. Confidence: 0.85
- For environment / runtime misconfigurations (e.g. user has a GPU but installed the CPU-only build of a native dependency), surface an actionable hint with the exact command the user must run — do NOT auto-fix. Auto-installing system-level dependencies silently crosses a permission/threshold the user didn't grant, and is a class of action that should stay in the user's hands. Show the diagnosis + the fix command in the UI; let them click-to-copy and run it. Confidence: 0.85
- When the user combines an investigation/diagnosis question with a planning/feature request in the same message ("is X happening, and also I want Y"), do both in one turn: give the answer to the diagnosis *and* draft a plan for the feature. Don't punt either part to a follow-up. The user batches related concerns on purpose. Confidence: 0.85
- When offering question-prompt options during plan-mode, if the assistant has a clearly-stated "Recommended" default, the user tends to accept it (often all of them in a single round). Phrase the recommended option's description so the rationale is concrete and concrete-benefit-shaped — not just "this is what I would pick". The user is happy to defer when the reasoning is visible. Confidence: 0.8
- For cross-cutting technical knobs that affect multiple things (e.g. layer-offload count, compute backend, output format), default to a single global setting rather than per-tier / per-model copies, unless the user explicitly asks for differentiation. One knob is easier to reason about; per-tier copies are justified only when tiers need genuinely different values. Confidence: 0.8
- For technical / power-user controls in the UI, prefer a preset-dropdown (Auto / Conservative / Aggressive / Off) plus a small numeric input next to it as an escape hatch. The presets give guided quick choices; the numeric input lets the user dial in a specific value without being forced to use the slider-only UI. Avoid pure sliders for discrete integer parameters. Confidence: 0.8
- After a plan is approved, the user expects the assistant to immediately begin execution and verification, not ask for another confirmation. Plan approval IS the green light — treat the "Ready for `/goal`" / "exit_plan_mode" handoff as authorization to proceed and run the verification harness in the same turn. Re-prompting "Want me to execute?" is a wasted round. Confidence: 0.9
- When execution hits a blocker that prevents final verification (env issue, missing tool, network), report status as a structured breakdown: ✅ Completed / ⚠️ Stuck / 🔜 Remaining — with the concrete unblock path (e.g. "run `cargo test --lib` on a machine where the test binary can load, and report back"). Don't just stop at "I can't finish"; give the user a clear status they can act on. Confidence: 0.85
- When offering two unblock paths (keep digging vs hand back to user), let the user pick — don't auto-pick (a) and don't auto-pick (b). The user wants agency on the fork between "assistant time spent debugging the environment" vs "user runs the verification themselves". Confidence: 0.8
- Uses a single-word "Continue." as the standard green-light to authorize the assistant to keep going after a structured status report that offered fork options. The user is delegating the fork decision back to the assistant's judgment — pick the more efficient path (keep digging vs. hand back) and proceed without re-asking. Confidence: 0.85
- When the test harness is broken by an environmental issue (e.g. test binary won't load due to a missing DLL on this host), prefer writing minimal standalone Rust programs that re-implement the new logic and running them with plain `rustc -O` + execution as a verification fallback. Skip the broken test binary, verify the algorithm directly, document the limitation honestly. Confidence: 0.85
- For Windows test-binary load failures (STATUS_ENTRYPOINT_NOT_FOUND 0xc0000139), use Python with `ctypes` + manual PE parsing to walk the import/delay-load directories, data directories, and `.pdata` exception tables to find the missing entry point — before reaching for stub DLLs or rebuilding. Reading the PE directly is faster and more diagnostic than guessing. Confidence: 0.8
- When a test binary fails to load (env-level DLL/runtime issue) but the underlying change is small and self-contained, treat `cargo build` + `cargo check --all-targets` as sufficient type-check verification — don't burn a round trying to fix the env just to run tests. Confirm "tests compile cleanly" via `--all-targets`, document the env issue as pre-existing, and move on. Confidence: 0.85
- For bug reports phrased as observed behavior with an implied question ("I don't know if it's working... can you figure out why?"), the user expects a full root-cause investigation that traces the symptom back to specific code paths (file:line), not a quick "is it working?" yes/no. Treat the implicit "why?" as the actual ask. Confidence: 0.85# Taste
- Writes goals as structured markdown specs with sections like `## 🎯 Goal`, `## 📋 Step-by-Step`, and explicit verification/acceptance criteria (e.g. "peak VRAM < 3.0 GB"). Confidence: 0.9
- Prefers plan-first workflow: explore codebase, draft a plan file (e.g. under `~/.commandcode/plans/`), and get explicit user approval via question prompts before making edits. Confidence: 0.9
- Likes plans that include an explicit "Out of Scope / Not Changing" section to bound the work. Confidence: 0.85
- When offered trade-off choices via `ask_user_question`, prefers the option that tracks real/observable values over cosmetic or spec-aligned ones (e.g. picked "track on-disk size" over "match goal text"). Confidence: 0.7
- Specifies exact numeric targets (byte counts, VRAM ceilings, file paths) rather than vague goals. Confidence: 0.85
- Primary development platform is Windows: uses `%APPDATA%` paths, PowerShell scripts (`.ps1`), and `nvidia-smi` for VRAM verification. Confidence: 0.95
- Target consumer GPU is NVIDIA RTX 2050 with 4 GB VRAM — work should be validated against this constrained-memory budget. Confidence: 0.9
- Project appdata layout: `%APPDATA%\reflow\` with subfolders `config\` (settings.json), `database\` (history.db), and `models\` (downloaded weights). Treat `config` and `database` as sacred — never delete. Confidence: 0.95
- Prefers `snapshot_download` with an explicit `allow_patterns` whitelist (config, tokenizer, preprocessor, processor, `*.safetensors`, `*.json`) over the default catch-all — avoids pulling duplicate `.bin`/flax/onnx artifacts and keeps on-disk footprint minimal. Confidence: 0.85
- Prefers INT8 weight-only quantization via `torchao` (`TorchAoConfig(Int8WeightOnlyConfig())`) over BF16 for ASR on small VRAM; load strategy should try INT8 first, then BF16, then CPU — never attempt to allocate uncompressed BF16 layers on a 4 GB card. Confidence: 0.9
- Progress / size specs (`approx_bytes` in Rust, `expected_bytes_for()` in Python) should agree on the same numeric values and track real on-disk byte counts. Confidence: 0.8
- After a download/install completes, re-measure the on-disk directory size and update the spec to the *actual* byte count (e.g. 1,503 MB vs the 1,500 MB estimate) so the progress bar denominator matches reality exactly — don't ship estimates once the truth is known. Confidence: 0.85
- Once the user has given an explicit "yes" to a non-destructive action (downloading files, running a script, etc.), proceed and execute — don't keep re-asking with another `ask_user_question` for the same action. Save confirmation prompts for the initial scope; subsequent "yes" means "go". Confidence: 0.85
- When the assistant *can* perform an action itself (e.g. running a Python script that calls `huggingface_hub.snapshot_download` to populate `%APPDATA%`), the user prefers the assistant to do it directly rather than handing back a command the user must run — "download these two models in my application" meant *do* the download, not "here's how you do it". Confidence: 0.9
- When the user asks to "fix it and also [do the thing]", bundle both into one turn: ship the code fix, build/verify it, and execute the downstream action (e.g. trigger the model download) before reporting back. Don't return a fix and leave the side-effect for the user to re-trigger from the UI. Confidence: 0.85
- For Windows shell quirks (PowerShell eating `$_`, escaping embedded Python), write the logic to a `.ps1` / `.py` file under the scratchpad and run it with `-File`, rather than fighting inline `-Command` quoting. Confidence: 0.8
- One-shot destructive cleanup (deleting legacy model caches) should be a separate idempotent PowerShell script, not wired into the app — with sanity checks that `config` and `database` exist before deleting anything. Confidence: 0.85
- Rust struct field order in `ModelSpec` follows `id, dir_name, repo, label, approx_bytes` — preserve this order when editing. Confidence: 0.7
- Uses Tauri (Rust backend + React/TypeScript frontend) for desktop apps. Confidence: 0.95
- Python sidecar pattern: model runtime lives in `model-runtime/qwen3_asr_runtime.py` and is invoked from Rust via Tauri commands; weight installs happen inside the sidecar using `huggingface_hub.snapshot_download`. Confidence: 0.85
- Verification harness: `scripts/test_sidecar.py` is the headless check for sidecar behavior. Confidence: 0.8
- When a plan mixes code edits with environment actions (running scripts, downloading models, modifying `%APPDATA%`), clearly separate "what I changed in code" from "what still needs to run on your machine" — and call out the concrete commands/steps the user must execute. Don't conflate "I wrote the script" with "the script ran". Confidence: 0.85
- Before running destructive or system-modifying actions (deleting files in `%APPDATA%`, triggering downloads), ask for explicit confirmation first. Confidence: 0.85
- When reporting bugs, the user includes a screenshot/image of the current UI state alongside a chronological narrative of the failure (what they clicked → what they did next → what they observed). Investigation should anchor to both the visual evidence and the timeline, not just one. Confidence: 0.85
- Bug reports are framed as a two-part question: "what is happening?" (root-cause) + "is the code working properly?" (defensive check for related issues). Don't just fix the reported symptom — also audit nearby code paths for the same class of bug. Confidence: 0.8
- For non-trivial bug investigations spanning both Rust backend and TypeScript frontend (e.g. download flow), delegate a thorough multi-file investigation to an `explore` subagent with explicit instructions to trace the full event/state flow (initiation → progress events → state updates → UI re-render) and call out likely causes. Confidence: 0.85
- UI state badges (Installed / Download / Downloading / etc.) must reflect the actual on-disk reality, not be a function of multiple orthogonal conditions (e.g. don't require both weights AND runtime to be present to show "Installed" — that's misleading and confuses users when only one piece is missing). Show the *real* sub-state (e.g. "Weights only" with an actionable hint) rather than pretending nothing is downloaded. Confidence: 0.9
- When shipping to an open-source audience (GitHub release, public users), download/install UX must be robust end-to-end: don't show a "Download" button when the file is already on disk, don't re-trigger downloads from stale UI clicks, and surface actionable hints (e.g. "runtime binary missing at path X") instead of generic error states. The bar is "works perfectly fine for a stranger". Confidence: 0.9
- For partial-state scenarios (e.g. weights present but runtime missing), prefer adding a new explicit field to the status response (e.g. `runtime_installed`) over overloading existing boolean fields. Keeps each field's semantics clean and lets the UI compose them into accurate badges. Confidence: 0.85
- Download/installation status APIs should report state per-unit (per-tier, per-model) rather than only for the currently-active unit. Otherwise switching active selection makes other downloaded units appear "not installed" even when their files are on disk. Confidence: 0.85
- When a user shows a screenshot of broken UI behavior, the visual evidence should be the anchor for reproduction: trace the rendered branch back to its state derivation and the state derivation back to the source of truth on disk. The bug is almost always a mismatch between what the code thinks is true and what the file system shows is true. Confidence: 0.9
- When the user references an attached image in a bug report ("screenshot is not working", "see this image"), read the actual contents of the image file to interpret intent — the user's phrasing is often loose and may conflate "the attached evidence" with "a screenshot feature is broken". The visual content of the attached file is the ground truth, not the literal words. Don't reflexively interpret image-attach language as a request for a new screen-capture feature. Confidence: 0.85
- In plan files for bug-fix work, include a dedicated "Root causes" / "Bug analysis" section with code-level references (file paths, function names, line numbers) that traces each reported symptom back to the actual source — not just a flat "Steps to fix" list. Plans that pair diagnosis with remediation are accepted as the answer; plans that skip diagnosis feel shallow. Confidence: 0.85
- Uses the `/plan` slash command to explicitly request plan-mode entry for non-trivial investigation/fix work. Treat `/plan` as a strong signal to enter plan mode, explore thoroughly, and produce a structured plan file before any edits — even when the user also asks diagnostic questions in the same message. Confidence: 0.9
- For ML model runtimes (LLM, ASR, etc.), default to automatic GPU/CPU selection: use the GPU when one is present and the binary supports it, fall back transparently to CPU when not. The user should never get stuck because their hardware doesn't match the runtime's hard-coded acceleration flag. Mirror the audio sidecar's `pick_device("auto")` semantics across any new ML backend. Confidence: 0.9
- For card-style UI containers (tier lists, model pickers, item rows) where metadata text, status badges, and action buttons all share a single row, prefer a vertical stacked layout: header row (icon + title + status badge), then a metadata row, then a separate bordered sub-card for any long status/runtime info, then a footer row for action buttons separated by a subtle top-border divider. Cramped horizontal flex layouts where long filenames or runtime strings compete for the same wrap-line as the action stack produce visible overlap and "overloaded" text — vertical stacking with explicit sub-cards prevents the wrap collision. Confidence: 0.85
- When a user-requested execution mode (e.g. GPU) fails at launch because the binary/runtime wasn't built with the required support, fall back silently to the next-best mode (CPU) rather than surfacing a hard error. Log a warning so it's debuggable, but the user-facing experience should be "it just works" on whatever hardware they have. Confidence: 0.85
- Compute-backend settings should apply to *all* model runtimes in the project, not just one (e.g. a single `compute_backend` setting should govern both ASR and LLM engines). Avoid wiring the same dropdown twice or having per-model backend configs. Confidence: 0.85
- Track the actual execution mode the running child process was started with (e.g. `active_mode: RwLock<Option<LlamaMode>>`) and surface it to the UI via a `backend` label field. Don't infer mode from hardware detection at status-report time — report what *actually* loaded, since launch-time fallback may have changed the mode. Confidence: 0.85
- For GPU acceleration in cross-platform desktop apps, prefer **Vulkan** as the default build over CUDA/ROCm: it works on NVIDIA, AMD, and Intel ARC without per-vendor separate runtime DLLs, and ships as a small (~34 MB) self-contained download. CUDA is only worth the extra 146-250 MB + cudart overhead for power users who explicitly opt in. Confidence: 0.85
- For runtime/binary installers shipped to end users, prefer a single-command auto-install on first use (e.g. when the user first picks a feature that needs the runtime) over a separate "Install runtime" button — but also surface an explicit "Install" / "Reinstall" button in settings for cases where auto-install was dismissed, failed, or the user wants to swap builds. Default = friction-free; escape hatch = always available. Confidence: 0.85
